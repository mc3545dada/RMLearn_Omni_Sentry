v主线1，是否dbus未初始化（未启用HAL_DMA_Start接受
	在main函数116行开始断点
--答：初始化成功了，dma初始化也成功了



主线2，是否开启了dma接受但uart接受未成功执行（it.c中中断服务函数未成功启动自定义函数
	首先于it.c测试canrx是否能成功断点，如果能，类比uart3逐步断点
	---由于没有遥控器，不能确定是否是因为没有传输信号导致的不能进入中断



主线3，是否是由于未进行dbus头文件中#ifndef而导致的其他程序不能正常引入头文件
	先运行一下，再边对比其他.h文件边从dbus.h文件里测试
	



支线1，用串口调试发送每个阶段的返回值测试，

	v2，断点调试
		---可用





问题1,为什么没进串口中断但是一直能接受串口发送的信息（同理为什么不进遥控器中断）